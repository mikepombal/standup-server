type Query {
    info: String!
    listUsers: [User!]!
}

type User {
    username: ID!
    firstname: String!
    surname: String!
    role: Role!
}

enum Role {
    SUPERADMIN
    ADMIN
    BASIC
    NONE
}
