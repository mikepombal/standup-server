type User {
    username: ID! @id
    createdAt: DateTime! @createdAt
    firstname: String!
    surname: String!
    role: Role! @default(value: NONE)
}

enum Role {
    SUPERADMIN
    ADMIN
    BASIC
    NONE
}
