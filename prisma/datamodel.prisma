type User {
    username: ID! @id
    createdAt: DateTime! @createdAt
    firstname: String!
    surname: String!
    password: String!
    role: Role! @default(value: NONE)
}

enum Role {
    SUPERADMIN
    ADMIN
    BASIC
    NONE
}
